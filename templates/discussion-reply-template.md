# Discussion Reply Template

Use this as a base, then customize heavily.

---

Thanks for sharing details. Based on your setup (`<runtime/version>`), this looks like `<likely root cause>`.

Try this:

1. `<step 1 with exact command>`
2. `<step 2 with exact command>`
3. `<step 3 with exact command>`

Verify:

- You should see: `<expected output/log>`
- Then run: `<verification command>`

If it still fails, try:

1. `<fallback step 1>`
2. `<fallback step 2>`

If you can share `<one missing detail>`, I can help narrow it further.

If this solved it, feel free to mark it as the accepted answer so others can find it quickly.

---

## Keep or delete before posting

- Remove placeholders
- Keep only relevant commands
- Keep tone helpful and concise
- Ensure commands are safe
